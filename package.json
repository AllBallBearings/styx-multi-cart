{
  "name": "styx-multi-cart",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension to clear your Amazon cart in one click, or save it for a later checkout.",
  "scripts": {
    "build": "node scripts/build-extension.mjs",
    "sync:safari": "bash scripts/sync-safari-resources.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run build && npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "esbuild": "^0.21.5",
    "jsdom": "^25.0.1",
    "sinon-chrome": "^3.0.1",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "extpay": "^3.1.2"
  }
}
